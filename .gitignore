node_modules/
dist/
coverage/
*.tsbuildinfo

# OS noise
.DS_Store
Thumbs.db

# Local runtime data. Never commit an index or a prepared model.
.mulat/
mulat-verify-*/

# Local environment overrides
.env
.env.local

# Command Code. The project skill in .commandcode/skills/ is team-shared and IS
# committed. Personal taste and the generated permission allowlist are not: an
# allowlist would widen what someone else's agent may run.
.commandcode/taste/
.commandcode/settings.json
.commandcode/settings.local.json

# Generated synthetic fixtures (regenerate with `npm run fixture:generate`)
fixtures/sample/
